package com.example.orientation_app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background & Surface ──
val DarkBackground = Color(0xFF0A0F1C)
val DarkSurface = Color(0xFF131829)
val DarkCard = Color(0xFF1A2035)
val DarkCardBorder = Color(0xFF252B3D)

// ── Primary Accent (Teal) ──
val TealPrimary = Color(0xFF2DD4BF)
val TealDark = Color(0xFF14B8A6)
val TealGlow = Color(0x262DD4BF)   // 15% alpha for glow effects

// ── Text ──
val TextWhite = Color(0xFFFFFFFF)
val TextGray = Color(0xFF7B8199)
val TextMuted = Color(0xFF525870)

// ── Components ──
val BottomNavBg = Color(0xFF0D1219)
val BottomNavIcon = Color(0xFF3B4261)

// ── Accent: positive / green ──
val GreenAccent = Color(0xFF34D399)
val GreenBadgeBg = Color(0x2634D399)  // 15% alpha

// ── Input fields ──
val InputFieldBg = Color(0xFF161C2E)
val InputFieldBorder = Color(0xFF1F2640)

// ── Score card ──
val ScoreCardBg = Color(0xFF1C2237)

// ── Button accent (periwinkle from design) ──
val PeriwinkleButton = Color(0xFF8B9CF7)
